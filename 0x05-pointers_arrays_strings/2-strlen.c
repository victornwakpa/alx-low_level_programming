#include "main.h"

/**
 * _strlen - the function for this program
 * Description: For the funcion
 * @s: the parameter
 * Return: returns the length of a string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
