#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * _strlen - gets the length of a string
 * @s: the string to be checked
 * Return: the length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
